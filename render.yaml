services:
  - type: web
    runtime: node
    name: king
    plan: free
    buildCommand: npm ci && npm run build && test -f dist/index.html
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"
